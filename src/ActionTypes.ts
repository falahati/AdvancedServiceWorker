namespace AdvancedServiceWorker {
    export enum ActionTypes {
        Notification = "NOTIFICATION",
        ClearCache = "CLEAECACHE",
        Preload = "PRELOAD",
        Reload = "RELOAD",
        Broadcast = "BROADCAST"
    }
}